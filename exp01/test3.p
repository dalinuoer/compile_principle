
{
  if (a < b)
    a = a + b;
  else if (a > b)
    a = a - b;
  else if (a == b)
    a = a * b;
  else (a != b)
    a = a / b;

  _ab_c = 100;
}

// 回车的格式应该是LR，而不是CRLR

while a >= -1.2E-2
do b<=2

/* 请注意：测试文件的格式必须符合要求，
   比如，该文件要求的格式是UNIX格式。*/
